import React from 'react';
import Login from '../../../components/screens/auth/Login';
import FirstTime from '../../../components/screens/auth/FirstTime';

export const ReadLogin = () => {
    return (
        <Login />
    );
}

export const ReadFirsttime = () => {
    return (
        <FirstTime />
    );
}